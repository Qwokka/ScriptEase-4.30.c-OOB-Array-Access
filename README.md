# Overview

These are the supporting files for an upcoming article on exploiting the ScriptEase javascript interpreter from 1998 (Assuming I ever develop a work ethic and write the article).

The POC demonstrates a very convenient OOB array access by tricking the interpreter into believing the size of a Buffer object is larger than its actual size.
